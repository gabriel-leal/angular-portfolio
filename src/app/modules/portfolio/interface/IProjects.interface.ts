export interface Iprojects {
    src: string
    alt: string
    title: string
    width: string
    height: string
    description:string
    links: [
        {
            name:string
            site:string
        }
    ]
}